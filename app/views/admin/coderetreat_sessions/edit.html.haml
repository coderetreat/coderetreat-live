%h2
  =@coderetreat.location

%h2 Update The Constraint For This Session

=form_for @session, url: coderetreat_session_path(coderetreat_id: @coderetreat.admin_token, id: @session.id) do |f|
  =f.submit "Update"
  %br
  =f.label :constraints, "With these constraints (optional)"
  %br
  =f.text_area :constraints
